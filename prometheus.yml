global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'docker_metrics'
    static_configs:
      # Подключаемся к контейнеру cAdvisor, который работает на порту 8080 внутри сети Docker
      - targets: ['cadvisor:8080']