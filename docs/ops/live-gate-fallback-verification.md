# Live gate fallback verification

This temporary verification note is used to trigger the normal PR and post-merge verification path after the server live gate fallback was updated.

It does not change runtime behavior.
