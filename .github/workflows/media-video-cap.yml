name: Media Video Cap Agent

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    name: Media cap compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: echo "Media video cap agent is disabled for automatic mutation; production checks cover push safety."
