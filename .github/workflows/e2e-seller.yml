name: Seller Production E2E Tests

on:
  workflow_dispatch:
  schedule:
    # Runs daily at 07:00 UTC (01:00 CST)
    - cron: '0 7 * * *'

permissions:
  contents: read

concurrency:
  group: e2e-seller-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  e2e-seller:
    name: Run seller production E2E smoke
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      BASE_URL: https://mercasto.com
      E2E_SELLER_EMAIL: ${{ secrets.E2E_SELLER_EMAIL }}
      E2E_SELLER_PASSWORD: ${{ secrets.E2E_SELLER_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npm run e2e:install

      - name: Verify required secrets are present
        run: |
          set -euo pipefail
          missing=()
          [ -z "${E2E_SELLER_EMAIL}" ]   && missing+=("E2E_SELLER_EMAIL")
          [ -z "${E2E_SELLER_PASSWORD}" ] && missing+=("E2E_SELLER_PASSWORD")
          if [ ${#missing[@]} -gt 0 ]; then
            echo "❌ Missing required secrets: ${missing[*]}" >&2
            echo "Set them in Settings → Secrets → Actions" >&2
            exit 1
          fi
          echo "✅ All required secrets are present."

      - name: Run read-only seller production smoke
        run: CI=1 npx playwright test tests/e2e/seller-production-smoke.spec.js --workers=1 --retries=1 --reporter=list

      - name: Confirm full lifecycle and payment E2E remain isolated from production
        run: bash scripts/isolated-launch-e2e-gate.sh

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-seller-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 14
