name: Manual server gate

on:
  workflow_dispatch:
    inputs:
      operation:
        description: 'Read-only gate to run'
        required: true
        default: 'status'
        type: choice
        options:
          - status
          - verify_quick
          - security_smoke
          - seo_aeo_smoke
  workflow_run:
    workflows:
      - Production checks
    types:
      - completed

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

concurrency:
  group: manual-server-gate
  cancel-in-progress: false

jobs:
  server-gate:
    name: Manual server gate
    runs-on: [self-hosted, linux, docker]
    timeout-minutes: 45
    if: >-
      ${{ github.event_name == 'workflow_dispatch' ||
          (github.event.workflow_run.conclusion == 'success' &&
           github.event.workflow_run.event == 'pull_request' &&
           github.event.workflow_run.head_repository.full_name == github.repository) }}
    steps:
      - name: Verify trusted production checkout
        shell: bash
        run: |
          set -euo pipefail
          test -d /var/www/mercasto/.git
          test -f /var/www/mercasto/scripts/server-operator.sh
          cd /var/www/mercasto
          branch="$(git rev-parse --abbrev-ref HEAD)"
          if [[ "$branch" != main ]]; then
            echo "::error::Production checkout is on branch '$branch', expected 'main'."
            exit 1
          fi

          dirty="$(git status --porcelain=v1 --untracked-files=all)"
          if [[ -n "$dirty" ]]; then
            echo "::error::Production checkout is dirty. Do not edit /var/www/mercasto directly; use a separate worktree and keep temporary backups outside the repository."
            printf '%s\n' "$dirty" | sed -n '1,80p'
            exit 1
          fi

      - name: Resolve read-only operation
        id: operation
        shell: bash
        env:
          REQUESTED_OPERATION: ${{ github.event.inputs.operation || 'verify_quick' }}
        run: |
          set -euo pipefail
          case "${REQUESTED_OPERATION}" in
            status|verify_quick|security_smoke|seo_aeo_smoke) ;;
            *) echo "Unsupported operation: ${REQUESTED_OPERATION}" >&2; exit 64 ;;
          esac
          echo "value=${REQUESTED_OPERATION}" >> "${GITHUB_OUTPUT}"

      - name: Run trusted server-resident gate
        shell: bash
        env:
          OPERATION: ${{ steps.operation.outputs.value }}
        run: |
          set -euo pipefail
          cd /var/www/mercasto
          compose_env_file="$(mktemp)"
          trap 'rm -f "$compose_env_file"' EXIT
          DB_PASSWORD=ci \
          REDIS_PASSWORD=ci \
          GRAFANA_PASSWORD=ci \
          COMPOSE_ENV_FILE="$compose_env_file" \
            bash scripts/server-operator.sh "${OPERATION}"

      - name: Capture runner health
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          cd /var/www/mercasto
          bash scripts/server-operator.sh runner_health
